# Руководство по интеграции ML (TensorFlow.js + scikit-learn)

**Дата:** 2025-01-24  
**Статус:** ✅ **100% ИНТЕГРИРОВАНО**

---

## 🎯 Что добавлено

### 1. TensorFlow.js для браузера

**Файлы:**
- `customizations/aeronavigator/features/ai-ml/TensorFlowML.ts` - модуль TensorFlow.js
- `customizations/aeronavigator/features/ai-ml/MLIntegration.ts` - интеграция

**Возможности:**
- ✅ Обучение моделей обнаружения аномалий (автоэнкодеры)
- ✅ Обучение моделей прогнозирования временных рядов (LSTM)
- ✅ Кластеризация данных (K-means)
- ✅ Работает полностью в браузере
- ✅ Не требует внешних сервисов
- ✅ Безопасно для РФ

**Зависимости:**
```json
{
  "@tensorflow/tfjs": "^4.15.0",
  "@tensorflow/tfjs-core": "^4.15.0"
}
```

### 2. scikit-learn для backend

**Файлы:**
- `backend/ml_services.py` - ML сервисы на scikit-learn
- `backend/ml_api.py` - API endpoints для ML

**Возможности:**
- ✅ Isolation Forest для обнаружения аномалий
- ✅ Random Forest для прогнозирования временных рядов
- ✅ K-means и DBSCAN для кластеризации
- ✅ PCA для снижения размерности
- ✅ Работает локально на сервере
- ✅ Не требует внешних сервисов
- ✅ Безопасно для РФ

**Зависимости:**
```
scikit-learn>=1.3.0
numpy>=1.24.0
scipy>=1.10.0
```

---

## 🚀 Использование

### Frontend (TensorFlow.js)

```typescript
import { trainAnomalyDetectionModel, detectAnomaliesWithModel } from './TensorFlowML';

// Обучение модели
const model = await trainAnomalyDetectionModel(data);

// Обнаружение аномалий
const anomalies = await detectAnomaliesWithModel(model, newData);

// Очистка
model.dispose();
```

### Backend API (scikit-learn)

```bash
# Обучение модели обнаружения аномалий
curl -X POST http://localhost:8080/api/v1/ml/anomaly-detection/train \
  -H "Content-Type: application/json" \
  -d '{
    "data": [[1, 2], [2, 3], [3, 4]],
    "contamination": 0.1
  }'

# Предсказание аномалий
curl -X POST http://localhost:8080/api/v1/ml/anomaly-detection/predict \
  -H "Content-Type: application/json" \
  -d '[[1, 2], [2, 3], [100, 200]]'
```

### Автоматическая интеграция

Все AI/ML функции автоматически используют ML методы если доступны:

```typescript
// Автоматически использует TensorFlow.js или scikit-learn если доступны
const anomalies = await datalens.ai.anomalyDetection(data, {
  useML: true, // По умолчанию true
  method: 'auto' // Автоматический выбор лучшего метода
});

// Автоматически использует ML для прогнозов
const predictions = await datalens.ai.predictiveAnalytics(data, 30, {
  useML: true,
  method: 'auto'
});
```

---

## 📊 Приоритет методов

1. **Backend scikit-learn** (если доступен) - самый мощный
2. **TensorFlow.js** (если доступен) - хороший баланс
3. **Статистические методы** (всегда доступны) - fallback

---

## ✅ Безопасность для РФ

**Все методы:**
- ✅ Работают локально
- ✅ Не требуют внешних сервисов
- ✅ Не отправляют данные за пределы РФ
- ✅ Используют только бесплатные библиотеки
- ✅ Open-source решения

---

## 🎯 Итог

**Добавлено на 100%:**
- ✅ TensorFlow.js для браузера
- ✅ scikit-learn для backend
- ✅ Автоматическая интеграция
- ✅ API endpoints
- ✅ Документация

**Готово к использованию!**

