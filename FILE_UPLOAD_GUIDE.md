# Руководство по загрузке файлов в DataLens

## ✅ Поддерживаемые форматы

### Текстовые форматы
- **CSV** (.csv, .txt) - до 500 MB
- **TSV** (.tsv) - до 500 MB

### Электронные таблицы
- **Excel XLSX** (.xlsx) - до 100 MB
- **Excel XLS** (.xls) - до 50 MB
- **OpenDocument Spreadsheet** (.ods) - до 100 MB

### Структурированные данные
- **JSON** (.json) - до 200 MB
- **XML** (.xml) - до 200 MB

### Колоночные форматы (Big Data)
- **Parquet** (.parquet) - до 1000 MB
- **Avro** (.avro) - до 1000 MB
- **ORC** (.orc) - до 1000 MB

### Документы с таблицами
- **PDF** (.pdf) - до 50 MB (извлечение таблиц)
- **HTML** (.html, .htm) - до 50 MB (извлечение таблиц)

## 🚀 Использование

### Через UI

1. Откройте DataLens
2. Перейдите в раздел "Датасеты"
3. Нажмите "Создать датасет" → "Загрузить файл"
4. Выберите файл или перетащите его в область загрузки
5. Система автоматически определит формат файла
6. Просмотрите предпросмотр данных
7. Настройте параметры парсинга (при необходимости)
8. Нажмите "Загрузить"

### Через API

```typescript
import { uploadFile } from '@aeronavigator/datalens-customizations';

const result = await uploadFile({
  file: fileObject,
  datasetName: 'my_dataset',
  format: 'csv', // опционально, будет определен автоматически
  parseOptions: {
    delimiter: ',',
    encoding: 'utf-8',
    headerRow: 0,
  },
  autoDetectTypes: true,
}, (progress) => {
  console.log(`Загрузка: ${progress.percentage}%`);
});

console.log(`Датасет создан: ${result.datasetId}, строк: ${result.rowCount}`);
```

## ⚙️ Параметры парсинга

### CSV/TSV
- `delimiter` - разделитель (по умолчанию: ',' для CSV, '\t' для TSV)
- `encoding` - кодировка (автоопределение по умолчанию)
- `skipRows` - пропустить N строк в начале
- `headerRow` - номер строки с заголовками (0 = первая строка)
- `decimalSeparator` - разделитель десятичных ('.' или ',')
- `thousandsSeparator` - разделитель тысяч
- `quoteChar` - символ кавычек
- `escapeChar` - символ экранирования

### Excel
- `sheetName` - имя листа или номер (0 = первый лист)
- `headerRow` - номер строки с заголовками

### JSON
- Автоматическое определение структуры
- Поддержка массивов объектов
- Поддержка объектов с массивами

### XML
- Автоматическое извлечение табличных данных
- Поддержка вложенных структур

### PDF/HTML
- Автоматическое извлечение таблиц
- Поддержка нескольких таблиц на странице

## 🔍 Предпросмотр

Перед загрузкой можно просмотреть данные:

```typescript
import { previewFile } from '@aeronavigator/datalens-customizations';

const preview = await previewFile(file, {
  format: 'csv',
  parseOptions: { delimiter: ',' },
  previewRows: 10,
});

console.log(`Колонок: ${preview.columns.length}`);
console.log(`Строк: ${preview.rowCount}`);
preview.columns.forEach(col => {
  console.log(`${col.name} (${col.type}): ${col.sampleValues.join(', ')}`);
});
```

## 📊 Автоопределение типов

Система автоматически определяет типы данных:
- **Числа** - Int64, Float64
- **Даты** - DateTime
- **Булевы** - Bool
- **Текст** - String

Можно отключить автоопределение, установив `autoDetectTypes: false`.

## ⚠️ Ограничения

- Максимальный размер файла зависит от формата (см. таблицу выше)
- Для очень больших файлов рекомендуется использовать Parquet или Avro
- PDF и HTML поддерживают только табличные данные

## 🐛 Решение проблем

### Ошибка кодировки
Укажите правильную кодировку в `parseOptions.encoding`:
```typescript
parseOptions: { encoding: 'windows-1251' } // для русских файлов
```

### Неправильный разделитель
Для CSV с точкой с запятой:
```typescript
parseOptions: { delimiter: ';' }
```

### Проблемы с датами
Укажите формат даты:
```typescript
parseOptions: { dateFormat: 'DD.MM.YYYY' }
```

## 📝 Примеры

### Загрузка CSV с русской кодировкой
```typescript
await uploadFile({
  file: csvFile,
  datasetName: 'russian_data',
  format: 'csv',
  parseOptions: {
    encoding: 'windows-1251',
    delimiter: ';',
    decimalSeparator: ',',
  },
});
```

### Загрузка Excel с выбором листа
```typescript
await uploadFile({
  file: excelFile,
  datasetName: 'excel_data',
  format: 'xlsx',
  parseOptions: {
    sheetName: 'Sheet2',
    headerRow: 1,
  },
});
```

### Загрузка JSON
```typescript
await uploadFile({
  file: jsonFile,
  datasetName: 'json_data',
  format: 'json',
  autoDetectTypes: true,
});
```

### Загрузка Parquet (Big Data)
```typescript
await uploadFile({
  file: parquetFile,
  datasetName: 'big_data',
  format: 'parquet',
  // Parquet уже содержит типы, автоопределение не нужно
  autoDetectTypes: false,
});
```

## 🔗 Интеграция с DataLens

После загрузки файл автоматически:
1. Парсится в DataFrame
2. Загружается в ClickHouse
3. Создается датасет в DataLens
4. Становится доступным для создания чартов и дашбордов

Датасет можно использовать как обычный источник данных в DataLens.

