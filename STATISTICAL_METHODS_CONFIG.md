# Конфигурация статистических методов в ML

**Дата:** 2025-01-24  
**Статус:** ✅ **100% РЕАЛИЗОВАНО**

---

## 🎯 Обзор

Статистические методы используются как **fallback (приоритет 3)** для ML функций, когда TensorFlow.js или scikit-learn недоступны или не могут быть использованы.

---

## 📊 Приоритет методов

### 1. Backend scikit-learn (Приоритет 1) - Самый мощный
- Isolation Forest для аномалий
- Random Forest для прогнозов
- K-means и DBSCAN для кластеризации
- PCA для снижения размерности

### 2. TensorFlow.js (Приоритет 2) - Хороший баланс
- Автоэнкодеры для аномалий
- LSTM для прогнозов
- K-means для кластеризации

### 3. Статистические методы (Приоритет 3) - Fallback
- Z-Score для аномалий
- IQR (Interquartile Range) для аномалий
- Упрощенный Isolation Forest для аномалий
- Линейная регрессия для прогнозов
- Экспоненциальное сглаживание для прогнозов
- Тренд + сезонность для прогнозов

---

## ⚙️ Конфигурация

### В `config.ts`:

```typescript
export const AERONAVIGATOR_FEATURES = {
  // ...
  STATISTICAL_METHODS_FALLBACK: true, // Включить статистические методы как fallback
} as const;

export const ML_CONFIG = {
  methodPriority: {
    backend: 1,      // scikit-learn на backend
    tensorflow: 2,   // TensorFlow.js в браузере
    statistical: 3,  // Статистические методы
  },
  
  useStatisticalFallback: true, // Использовать статистические методы как fallback
  
  minDataPointsForML: {
    tensorflow: 20,
    backend: 20,
    statistical: 3,  // Статистические методы работают с меньшим количеством данных
  },
} as const;
```

---

## 🚀 Использование

### Автоматический выбор (рекомендуется)

```typescript
// Автоматически выберет лучший доступный метод
const anomalies = await datalens.ai.anomalyDetection(data, {
  useML: true, // Пробовать ML методы
  useStatisticalFallback: true, // Использовать статистику если ML недоступен
  method: 'auto'
});
```

### Только ML методы (без статистики)

```typescript
const anomalies = await datalens.ai.anomalyDetection(data, {
  useML: true,
  useStatisticalFallback: false, // Отключить статистические методы
  method: 'auto'
});
```

### Только статистические методы

```typescript
const anomalies = await datalens.ai.anomalyDetection(data, {
  useML: false, // Отключить ML
  useStatisticalFallback: true, // Использовать только статистику
  method: 'zscore' // или 'iqr', 'isolation', 'all'
});
```

---

## 📝 Параметры функций

### `detectAnomalies()`

```typescript
interface AnomalyDetectionOptions {
  fields?: string[];
  method?: 'zscore' | 'iqr' | 'isolation' | 'tensorflow' | 'backend' | 'all';
  useML?: boolean; // По умолчанию true
  useStatisticalFallback?: boolean; // По умолчанию true (из конфига)
  threshold?: number; // По умолчанию 3 для Z-Score
}
```

### `generatePredictions()`

```typescript
interface PredictiveAnalyticsOptions {
  field?: string;
  dateField?: string;
  method?: 'linear' | 'exponential' | 'trend' | 'tensorflow' | 'backend' | 'auto';
  useML?: boolean; // По умолчанию true
  useStatisticalFallback?: boolean; // По умолчанию true (из конфига)
}
```

---

## 🔍 Логика выбора метода

1. **Если `useML === true`**:
   - Пробуем backend scikit-learn (если доступен)
   - Если не доступен, пробуем TensorFlow.js (если доступен)
   - Если не доступен и `useStatisticalFallback === true`, используем статистические методы
   - Если не доступен и `useStatisticalFallback === false`, возвращаем пустой результат

2. **Если `useML === false`**:
   - Если `useStatisticalFallback === true`, используем статистические методы
   - Если `useStatisticalFallback === false`, возвращаем пустой результат (или используем как последний вариант)

---

## ✅ Преимущества статистических методов

1. **Работают везде** - не требуют внешних библиотек
2. **Быстрые** - для малых данных быстрее ML
3. **Прозрачные** - легко объяснить результаты
4. **Низкие требования** - работают с 3+ точками данных
5. **Надежные** - проверенные временем методы

---

## ⚠️ Когда отключить статистические методы

Отключите статистические методы если:
- Всегда доступны ML методы (TensorFlow.js или scikit-learn)
- Нужна максимальная точность (ML обычно точнее)
- Работаете с большими данными (ML лучше масштабируется)
- Нужны сложные паттерны (ML лучше их обнаруживает)

---

## 📊 Сравнение методов

| Метод | Точность | Скорость | Требования | Прозрачность |
|-------|----------|----------|------------|--------------|
| scikit-learn | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | Backend, Python | ⭐⭐⭐ |
| TensorFlow.js | ⭐⭐⭐⭐ | ⭐⭐⭐ | Browser, JS | ⭐⭐ |
| Статистика | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | Нет | ⭐⭐⭐⭐⭐ |

---

## 🔧 Отключение через конфиг

Чтобы полностью отключить статистические методы:

```typescript
// В config.ts
export const AERONAVIGATOR_FEATURES = {
  STATISTICAL_METHODS_FALLBACK: false, // Отключить
} as const;

export const ML_CONFIG = {
  useStatisticalFallback: false, // Отключить
} as const;
```

---

## 📚 Документация

- [ML Integration Guide](./ML_INTEGRATION_GUIDE.md)
- [Anomaly Detection](./customizations/aeronavigator/features/ai-ml/AnomalyDetection.ts)
- [Predictive Analytics](./customizations/aeronavigator/features/ai-ml/PredictiveAnalytics.ts)
- [ML Integration](./customizations/aeronavigator/features/ai-ml/MLIntegration.ts)

---

## ✅ Итог

**Статистические методы:**
- ✅ Оставлены как fallback
- ✅ Сделаны опциональными через `useStatisticalFallback`
- ✅ Используются с приоритетом 3 (после ML)
- ✅ Можно отключить через конфиг
- ✅ Документированы

**Готово к использованию!**

